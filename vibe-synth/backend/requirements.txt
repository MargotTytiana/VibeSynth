# =============================================================================
# File: backend/requirements.txt
# Purpose: Python package dependencies for the Vibe-Synth backend service.
#          Pinned to minor versions for reproducible builds. Split into
#          sections: core framework, LLM providers, testing, and dev tooling.
#          Install with: pip install -r requirements.txt
# =============================================================================


# ---------------------------------------------------------------------------
# Core framework
# ---------------------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.32.1        # ASGI server with uvloop + httptools
pydantic==2.10.3                 # Data validation (FastAPI dependency)
pydantic-settings==2.7.0         # Settings management from env / .env file


# ---------------------------------------------------------------------------
# LLM providers  (install the one matching your settings.llm_provider)
# ---------------------------------------------------------------------------
openai==1.58.1                   # OpenAI + local OpenAI-compatible endpoints
anthropic==0.40.0                # Anthropic Messages API


# ---------------------------------------------------------------------------
# Async HTTP  (used by provider SDKs)
# ---------------------------------------------------------------------------
httpx==0.28.1
anyio==4.7.0


# ---------------------------------------------------------------------------
# Compiler / sandbox
# ---------------------------------------------------------------------------
# wasmtime is used by compiler/wasm_compiler.py to load and validate
# WASM modules produced by the Faust-to-WASM toolchain.
wasmtime==26.0.0


# ---------------------------------------------------------------------------
# Logging and observability
# ---------------------------------------------------------------------------
python-json-logger==3.2.1        # Structured JSON log output for production


# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.0           # async test support for pytest
pytest-cov==6.0.0                # coverage reporting


# ---------------------------------------------------------------------------
# Dev / lint (not needed in production Docker image)
# ---------------------------------------------------------------------------
ruff==0.8.4                      # Fast Python linter + formatter
mypy==1.13.0                     # Static type checker